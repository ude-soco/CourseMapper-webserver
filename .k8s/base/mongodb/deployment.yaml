apiVersion: apps/v1
kind: Deployment
metadata:
  name: mongodb
spec:
  replicas: 1
  strategy:
    type: Recreate
  selector:
    matchLabels:
      component: mongodb
  template:
    metadata:
      labels:
        component: mongodb
    spec:
      initContainers:
      - name: repair
        image: mongo
        command:
        - mongod
        - --repair
        volumeMounts:
        - mountPath: /data/db
          name: mongodb
      containers:
      - name: mongodb
        image: mongo
        args:
        - mongod
        - --bind_ip_all
        - --quiet
        ports:
        - containerPort: 27017
        volumeMounts:
        - mountPath: /data/db
          name: mongodb
        resources:
          requests:
            memory: 160Mi
            cpu: 100m
          limits:
            memory: 320Mi
            cpu: 1
      volumes:
      - name: mongodb
        persistentVolumeClaim:
          claimName: mongodb
