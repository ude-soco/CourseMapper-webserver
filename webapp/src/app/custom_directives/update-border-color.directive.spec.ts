import { UpdateBorderColorDirective } from './update-border-color.directive';

describe('UpdateBorderColorDirective', () => {
  it('should create an instance', () => {
    const directive = new UpdateBorderColorDirective();
    expect(directive).toBeTruthy();
  });
});
