export interface Material {
    _id: string;
    name: string;
    type?: string;
    url?: string;
    description?: string;
    courseId?: string;
    topicId?: string;
    channelId?: string;
   // userId: req.userId,
   //createdAt: Date.now(),
    //updatedAt: Date.now(),
    
  }
  